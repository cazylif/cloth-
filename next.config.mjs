/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains:['lh3.googleusercontent.com',"assets.adidas.com","localhost"]
  },
};

export default nextConfig;
